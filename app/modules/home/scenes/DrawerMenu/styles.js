import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 20,
    backgroundColor: '#fff',
  },
  titleContainer: {
    paddingTop: 10,
    paddingBottom: 10,
  },
  titleText: {
    fontSize: 20,
  },
});

export default styles;
